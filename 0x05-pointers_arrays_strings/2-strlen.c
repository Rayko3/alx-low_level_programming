#include "main.h"

/**
 * _strlen - returns the lenght of a string
 * @*s : pointer to a char
 * @s : is a char
 * Return: returns the lenght of the string
 */


int _strlen(char *s)
{
	return (sizeof(&s));
}
